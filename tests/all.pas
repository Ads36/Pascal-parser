program TestAll;
var
    x : integer,
    y : integer,
    z : integer,
    d : double,
    s : string; 

function strom(koren : integer) : string;
begin
    writeln("jsem funkcicka na stromecky");
end

function les(strom : integer) : string;
var
    lesik : integer;
begin
    {writeln("jsem funkcicka na lesiky");}
    if(lesik > 100) then 
        x := 5;
    else x := 89.5;
end

 
begin
    for i := 1 to 5 do
        i := i + 1;
    x := 5;
    y := (-4+(-7))*9 - 2 + 3;
    z := y / 7;
    if (x + 2) >= 7 then
     writeln('jupi');
    else 
        x :=5;
    while xyz >= 10 do
        begin
            writeln('Jeden pruchod cyklem');
        end;
    writeln('Zadej string');
    readln(s);
    writeln(s);
    writeln(y);
    x;
    writeln(x);
    s := lesik(101);
    writeln(s);
end.
